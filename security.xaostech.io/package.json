{
    "name": "xaostech-security",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "build": "echo 'no build required'",
        "deploy": "wrangler deploy --env production",
        "scan": "wrangler dev --local --test-scheduled"
    },
    "dependencies": {
        "hono": "^4.7.0",
        "zod": "^3.24.0"
    },
    "devDependencies": {
        "wrangler": "^4.0.0",
        "typescript": "^5.7.0",
        "@cloudflare/workers-types": "^4.0.0"
    }
}